## 2024-03-20 - Adding Accessible ARIA Labels to Input Controls
**Learning:** Adding translated localized text for `aria-label` properties on icon buttons inside input fields ensures proper context when navigating with screen readers.
**Action:** Always provide translated accessible text (`aria-label`) on utility buttons like clear search controls (`X` icons).
